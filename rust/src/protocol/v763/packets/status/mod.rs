//! Status state — server-list-ping flow.
//!
//! Skeleton only; the Rust mirror's US1 scope (offline connect → PLAY)
//! never touches STATUS, so concrete packets are deferred.

pub mod clientbound;
pub mod serverbound;
