<!-- SPECKIT START -->
Active feature: `002-bot-api`. For technologies, project structure,
shell commands, and other context, read the current plan at
`specs/002-bot-api/plan.md`. Companion artefacts in the same
directory: `spec.md`, `research.md`, `data-model.md`, `quickstart.md`,
and `contracts/`. Constitution lives at `.specify/memory/constitution.md`.

The previous milestone `001-protocol-foundation` is complete (codec,
framer, all 176 packets for protocol 763, Connection lifecycle,
WireLog capture + replay, US5 v764 port demo, Rust codec parity) and
serves as the foundation 002 builds on. See
`specs/001-protocol-foundation/` for its artefacts.
<!-- SPECKIT END -->
