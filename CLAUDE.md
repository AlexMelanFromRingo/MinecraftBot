<!-- SPECKIT START -->
Active feature: `004-full-bot-parity`. For technologies, project
structure, shell commands, and other context, read the current plan
at `specs/004-full-bot-parity/plan.md`. Companion artefacts in the
same directory: `spec.md` (with `## Clarifications`), `research.md`,
`data-model.md`, `quickstart.md`, and `contracts/api-surface.md`.
Constitution lives at `.specify/memory/constitution.md`.

Previous milestones: `001-protocol-foundation` (codec, framer, all 176
packets for protocol 763, Connection lifecycle, WireLog capture +
replay, US5 v764 port demo, Rust codec parity), `002-bot-api`
(Bot facade, World cache, walk_to + path planner, observation
snapshot, 20 Hz physics, hazard handling, drop/pickup), and
`003-rust-pyo3-bridge` (PyO3 facade `minecraft_bot_accel`, abi3
wheels for Linux + Windows, three-artefact release pipeline) are
all complete and serve as the foundation 004 builds on. See
`specs/001-protocol-foundation/`, `specs/002-bot-api/`, and
`specs/003-rust-pyo3-bridge/` for their artefacts.
<!-- SPECKIT END -->
